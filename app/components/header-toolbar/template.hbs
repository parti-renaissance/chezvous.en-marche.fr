<div class="header-toolbar__wrapper">
  <div class="header-toolbar__left">
    <Logo/>
  </div>

  <div class="header-toolbar__center">
    <button
      class="header-search"
      data-test-open-search
      {{action (mut showSearch) true}}
    >
      {{#if (media 'isMedium')}}
        {{svg-jar "search"}}
        <span class="header-search__label">Rechercher une commune</span>
      {{else}}
        {{svg-jar "search"}}
      {{/if}}
    </button>
  </div>

  <div class="header-toolbar__right">
    {{#if (media 'isLarge')}}
      <HeaderToolbar::feedback data-test-feedback />
    {{else}}
      {{#basic-dropdown as |dd|}}
        {{#dd.Trigger class="header-toolbar__feedback-button"}}
          {{svg-jar "more"}}
        {{/dd.Trigger}}

        {{#dd.Content}}
          <HeaderToolbar::feedback @vertical={{true}} data-test-feedback />
        {{/dd.Content}}
      {{/basic-dropdown}}
    {{/if}}
  </div>
</div>

{{#if showSearch}}
  <HeaderToolbar::modal
    @close={{action (mut showSearch) false}}
    @onSubmit={{action 'goToCity'}}
  />
{{/if}}
